{
  "name": "llm-game-vm",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "VM de juego determinista guiada por IR: el jugador escribe un deseo en lenguaje natural y un LLM (Gemini) reescribe el programa del juego.",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host",
    "harness": "tsx src/harness.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
